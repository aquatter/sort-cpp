#pragma once

constexpr int kNumColors = 32;

constexpr int kMaxCoastCycles = 3;

constexpr int kMinHits = 3;

// Set threshold to 0 to accept all detections
constexpr float kMinConfidence = 0.6f;

constexpr float kMinIOU = 0.1f;